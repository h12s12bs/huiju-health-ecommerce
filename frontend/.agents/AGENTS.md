# Project Rules

## GitHub 版本控制規則（必遵守）
- 所有軟體開發或程式開發專案，**必須上傳到 GitHub** 做版本控制。
- 每個專案應建立**獨立的 GitHub repository**，不可混在其他專案裡。
- 在虛擬機 (Cloud Shell / Codespace) 環境中，開發告一段落時一定要 **commit + push** 以免環境重置導致程式碼遺失。
- Repo 預設設為 **private**，除非使用者明確要求 public。
- GitHub 帳號 username：`h12s12bs`（已透過 `gh auth` 認證）。
